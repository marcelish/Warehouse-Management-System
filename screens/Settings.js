import React, { useState, useEffect, useRef } from 'react';
import { Animated, SafeAreaView, StyleSheet, Text, TextInput, TouchableOpacity, StatusBar, Alert, View, KeyboardAvoidingView, Platform, ScrollView, Keyboard} from 'react-native';

const Settings = () => {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text>Settings</Text>
        </View>
    );
}
export default Settings;
